using UnityEngine;
using System.Collections;

public class TextController : MonoBehaviour {

	// Use this for initialization
	void Start () {

	}
	
	// OnGUI is called once per frame
	void OnGUI () {
		if (GUI.Button (new Rect (1083, 567, 150, 50), "Previous")) {

				}

		if (GUI.Button (new Rect (1083, 618, 150, 50), "Close")) {

				}
	}
}
